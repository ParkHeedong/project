<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- mapper의 namespace 속성값은  Mapper 인터페이스와 동일한 이름을 준다 -->	
<mapper namespace="com.park.mapper.ReplyMapper">

	<insert id="insert">
		insert into tbl_reply (rno, bno, reply, replyer)
		values (seq_reply.nextval, #{bno}, #{reply}, #{replyer})
	</insert>
	
	
	<select id="read" resultType="com.park.domain.ReplyVO"	>
		select * from tbl_reply where rno = #{rno}
	</select>
	
	<delete id="delete">
		delete from tbl_reply where rno = #{rno}
	</delete>
	
	<update id="update">
		update tbl_reply set reply = #{reply}, updatedate = sysdate where rno = #{rno}
	</update>
	
	<select id="getListPaging" resultType="com.park.domain.ReplyVO"> <!-- 특정 게시물의 댓글을 가져옴 -->
		select rno, bno, reply, replyer, replydate, updatedate
		from tbl_reply
		where bno = #{bno} <!-- #{bno}가 @Param("bno")와 매칭되어서 사용된다 -->
		order by rno asc
	</select>
</mapper>